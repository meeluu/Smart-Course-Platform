# 项目文档 v1

> 这份文档是 `项目文档.md` 的**收窄版**：不新增想法，只做减法。
> 目的：先让一条闭环真正跑通，而不是先铺开一堆页面。
>
> **状态：已实现，文档与代码一致。** 第 4 节的步骤来源、第 7 节的归档去向、第 9 节的已定事项
> 都是实现之后回填的，改动代码时请一并更新本文档。
>
> 实现之前仓库里曾有一版更大的框架（项目驾驶舱 / 项目地图 / 证据与决定 / 项目启动，
> 外加四个直接动作、8 项状态板、8 类地图节点、断裂检测规则）。它按 `项目文档.md` 的完整设想做的，
> 范围偏大，现在整体归档在 `legacy-v0/`，不参与构建。
> **v1 就是把它砍到你和老师对话里的那条链路。**

---

## 1. 这个系统是什么

**给正在做项目的学生团队用的项目推进系统。**

以**项目当前状态**为核心：把材料变成理解，把理解变成下一步，把下一步变成证据，再回来更新状态。

它**不是**：

- 不是给学基础知识的学生用的（他们用 PPT 和课本就够）
- 不是课程资源库，不是题库
- 不是一个泛化的"问 AI"聊天框
- 不是自动写方案、自动写报告、替学生选模型的工具

老师那句"**因为我们这门课不是纯知识型的课程**"就是这条定位的依据：这门课的价值发生在学生做项目的过程中，所以系统要贴在项目上，而不是贴在知识点上。

---

## 2. 核心闭环（v1 只有这一条）

```
学生上传项目相关材料
        ↓
系统整理成四块：项目目标 / 交付内容 / 当前问题 / 待确认事项
        ↓
学生逐条确认或修改        ← 硬规则 1：未经确认不进项目状态
        ↓
系统给出当前最需要推进的 1~3 个步骤（每个步骤说明「为什么现在做」）
        ↓
每个步骤关联到具体问题，并推荐相关资料 + 说明「为什么有用」
        ↓
学生完成这一步，留下结果与证据（完成了什么 / 发现了什么 / 解决了哪个问题 / 还有什么不确定）
        ↓
系统据此更新项目状态 → 回到「给出 1~3 个步骤」
```

一句话概括每一环：

| 环节 | 系统做什么 | 学生做什么 |
| --- | --- | --- |
| 上传 | 接收材料 | 把项目相关材料交进来 |
| 理解 | 整理成四块草稿 | 确认或改写 |
| 下一步 | 给 1~3 步 + 理由 | 挑一步开始做 |
| 做 | 关联问题、推荐资料并说明用处 | 真的去做，把知识用到项目里 |
| 记录 | 更新项目状态 | 写清结果与证据 |

---

## 3. v1 的页面（3 个）

只有三个页面。**这是 v1 的硬边界**，任何"顺便也做一个"都往后放。

### 3.1 项目首页 —— 项目现在是什么状态

回答一个问题：**我们现在做到哪了、接下来做什么。**

页面内容（从上到下）：

1. **项目头**：项目名、团队、最近一次更新时间
2. **项目状态四块**（这是页面的主体）
   - 项目目标
   - 交付内容
   - 当前问题（列表；每一步会反向指出它在推进哪一条）
   - 待确认事项（列表，这是最容易被忽略但最有价值的一块）
   - 每一条都带可信程度标记：`已确认` / `系统推断` / `待确认`；还能点开看它来自哪份材料的哪一段
   - 四块**各自是独立的卡片**，内容多的那块不会把旁边那块撑出空白
3. **接下来最重要的 1~3 步**
   - 每一步：标题 + **为什么现在做** + 挂在哪条问题/待确认事项上 + 状态（待开始 / 进行中 / 已完成）+ 资料份数
   - 点进去 → 步骤详情
   - 区块右上角一个按钮：`重新判断下一步`，点完会告诉你「新增 N 步」还是「按现在掌握的情况，没有新的建议」
4. **进展记录**：时间线，按时间倒序列出每一步留下的结果与证据，以及状态因此发生了什么变化

> 这一页替代了原来的「项目驾驶舱 + 项目地图 + 证据与决定」三页。
> 项目地图（关系图、连线、断裂检测）在 v1 里**不做**，状态四块已经能回答同样的问题。

### 3.2 材料与理解 —— 把材料变成团队自己的理解

这是整个系统的入口，也是系统第一次展示"它读懂了什么"的地方。

页面内容：

1. **材料区**：已交材料的清单（名称、类型：需求说明 / 项目文档 / 数据说明 / 其他、上传时间、整理状态），以及一个「再交一份」入口
   - v1 **只支持直接粘贴文本**。上传 PDF / Word 并在浏览器里抽取正文需要额外的解析库，留到下一版
   - 整理是**按材料逐份进行**的：没整理过的材料上会标「待整理」，整理完标「已整理」
2. **整理结果区**：四块草稿
   - 项目目标（一段话）
   - 交付内容（列表）
   - 当前问题（列表）
   - 待确认事项（列表）
   - 每一条旁边标出它来自哪份材料的哪一部分
3. **确认动作**：每一条都有三个操作
   - `确认`
   - `修改`（改成团队自己的说法，然后确认）
   - `删掉`（系统猜错了）
4. **全部确认后**，页面给出通往"下一步"的入口，并明确提示：
   系统只有在你们确认之后，才会基于这份理解去判断下一步

> 这一页替代了原来的「项目启动快照」。
> 启动快照要求学生在没有材料的情况下凭空回答 7 个问题；这一版改成**先交材料，系统先读，学生只做确认与改写**——
> 门槛低得多，也更符合"刚拿到项目时学生手上其实只有材料"的真实情况。

### 3.3 步骤详情 —— 把知识用到项目里

回答一个问题：**这一步该怎么做，系统能帮我什么。**

页面内容：

1. **步骤头**：标题、状态、`为什么现在做`（这段文字要一直显示，不能折叠）
2. **这一步对应哪个问题**：把关联的问题原文摆出来，并给出这个问题现在的状态
3. **推荐资料**（这是 v1 里课程知识唯一出现的地方）
   - 每份资料：名称、类型（**课件 / 实验手册 / 项目规范 / 经验材料**，来自现有课程资料库）、页码、**为什么它对这个具体问题有用**（一句话）
   - 点开可看原文（复用现有的原文阅览器）
   - **资料没有「已读」这个状态**，不做打勾
4. **留下结果与证据**（这一步的收尾动作，四个字段）
   - 完成了什么（必填）
   - 发现了什么（必填，这条会直接写进进展记录）
   - 解决了哪个问题（两个选项：本步挂着的那个问题，或「没有解决任何一个问题」。**默认选中本步挂着的那条**）
   - 还有什么不确定（留空表示已经确定）
   - 另外有一个「先标记为进行中」按钮——状态只有 `进行中` 与 `已完成` 之分，**没有单独的"已开始阅读"这类进度**
5. 提交之后 → 回到项目首页，状态四块中对应的部分会被更新，进展记录里新增一条
6. 已完成之后，四个字段变成只读展示，不能再改——它是记录，不是草稿

> 这一页替代了原来的「四个直接动作（我卡住了 / 跑偏检查 / 下一步 / 问老师）」。
> v1 **只保留"下一步"这一个动作**，而且它不再是抽屉，直接长在首页上。
> "我卡住了""跑偏检查""问老师"留到 v1.1，理由见第 5 节。

---

## 4. 数据模型（v1 只有 7 个对象）

刻意保持小。每个字段都对应闭环里一个具体动作，没有"以后可能用得上"的字段。

### Project（项目）
| 字段 | 说明 |
| --- | --- |
| id / name / shortName | 项目标识、全名、短名（短名用于标题栏，避免中英混排被拦腰折断） |
| source | 赛题或来源，例如「崂山实验室需求对接会」 |
| team / members | 团队名与成员（姓名 + 分工） |
| phase | 课程进度背景，例如「项目第 3 周」 |
| totalWeeks / currentWeek | 总周数与当前周次——**whyNow 必须能引用它，否则建议就是空话** |
| startedAt / updatedAt | 开始与最近更新时间 |

v1 **只有一个项目**，不做项目列表、不做多团队、不做权限。

### Material（材料）
| 字段 | 说明 |
| --- | --- |
| id / name | 材料名 |
| type | 需求说明 / 项目文档 / 数据说明 / 其他 |
| text | 材料正文（v1 只支持直接粘贴文本） |
| note | 材料的补充说明，例如「2025-09-05 · 与需求方、助教」 |
| uploadedAt | 交进来的时间 |
| organizedAt | 系统整理过这份材料的时间；为空表示还没读，界面上标「待整理」 |

### UnderstandingItem（理解条目）
四块不是四个对象，而是同一个条目的四个分组：**一条理解就是一行**，各自带可信程度与出处。

| 字段 | 说明 |
| --- | --- |
| id | 条目标识 |
| key | 属于哪一块：`goal` 项目目标 / `deliverables` 交付内容 / `questions` 当前问题 / `toConfirm` 待确认事项 |
| text | 内容。确认或改写之后，它就是团队自己的说法 |
| confidence | `confirmed` 已确认 / `inferred` 系统推断 / `uncertain` 待确认 |
| origin | 谁写的：`ai` 系统 / `student` 学生 / `teacher` 老师 |
| source | 出处：`materialId` 哪份材料 + `locator` 哪一部分 + `quote` 原文片段。学生自己写的不带这一项 |
| at | 时间 |

### Step（步骤）
| 字段 | 说明 |
| --- | --- |
| id / title | 步骤标识与标题 |
| whyNow | **为什么现在做**。必填，不允许为空——空就说明这条建议没有依据 |
| questionId | 挂在哪一条理解上（必填）。指向一个 **UnderstandingItem 的 id**，它可能是一条「当前问题」，也可能是一条「待确认事项」——界面按类型分别显示 |
| status | 待开始 / 进行中 / 已完成 |
| resources | 推荐的资料，见下 |
| at | 生成时间 |

**步骤从哪来（四条来源，按优先级）**

这是 v1 里唯一「系统替学生想」的地方，所以来源也写死：

| 优先级 | 触发条件 | 为什么排在这个位置 |
| --- | --- | --- |
| ① | 有**待确认事项**还没确认 | 前提不定，后面每一步的排期都是假的 |
| ② | 上一步留下的 `stillUnsure` 非空 | 它会污染后面所有建立在这个前提上的结论 |
| ③ | 有已确认的**当前问题**没有任何结果落到它身上 | 这一步要推进的就是它 |
| ④ | 有**交付内容**没有任何进展 | 它是验收时要逐条对照的东西 |

最多输出 3 条。**写不出具体 `whyNow` 的步骤根本不生成**——不是"不展示"，是压根不出现在列表里。

### StepResource（推荐资料）
| 字段 | 说明 |
| --- | --- |
| id / title | 资料名 |
| docId / blockId | 指向课程资料库里的一份文档与一个证据块 |
| kind | 课件 / 实验手册 / 项目规范 / 经验材料 |
| page | 页码或节次 |
| whyUseful | **为什么它对这个具体问题有用**。必填，由「这份资料本身讲什么」+「这一步在问什么」拼成，不是泛泛的"这一章很相关" |

### Result（结果与证据）
| 字段 | 说明 |
| --- | --- |
| stepId | 属于哪一步 |
| didWhat | 完成了什么 |
| foundWhat | 发现了什么 |
| solvedWhich | 解决了哪个问题（某个 UnderstandingItem 的 id，或 `none` 表示没有解决） |
| stillUnsure | 还有什么不确定 |
| at | 提交时间 |

一个 Step 只对应一个 Result。提交之后：

- Step 变为 `done`
- 如果 `stillUnsure` 非空，**它会变成一条新的 `待确认事项`**（`confidence: uncertain`，出处写明来自哪一步）——这是规则 1 在"输出侧"的同一套做法：没确定的事不会被当成已知
- 如果 `solvedWhich` 是 `none`，进展记录里会多一条提醒：这一步没有解决任何一个已确认的问题

### ProgressEntry（进展记录）
| 字段 | 说明 |
| --- | --- |
| id / at | 记录标识与时间 |
| stepId / stepTitle | 属于哪一步 |
| summary | 这一步发现了什么（取 `foundWhat`，为空则取 `didWhat`） |
| changes | **状态因此发生的变化**，逐条列出。这是"结果真的改变了状态"在界面上的证据 |

提交一步就会追加一条 ProgressEntry，首页的时间线按时间倒序读它。

---

## 5. 四条硬规则（不可协商）

这四条不是文案，代码里要强制。

### 规则 1：系统整理的内容，必须经学生确认才能进项目状态
材料读出来的东西一律先是草稿，标 `系统推断`。学生确认或改写之后才变成 `已确认`。
系统永远不会把自己的推断当成项目事实。

### 规则 2：每条状态都必须标可信程度
`已确认` / `系统推断` / `待确认` 三档，界面上始终可见。
这一条是规则 1 能落地的前提——没有标注，就无法判断哪一条还能改。

### 规则 3：下一步只给 1~3 条，每条必须写清「为什么现在做」
不生成待办清单，不生成十几条任务。
`whyNow` 为空的步骤**根本不生成**——不是"不展示"，而是压根不出现在列表里。
**给不出理由的建议，说明系统也不知道为什么现在做**，那它就不该占用学生的注意力。

### 规则 4：资料没有「已读」，只有「用在哪一步的结果里」
这是"学生不是简单看完资料，而是要把知识用到项目中"在代码里的落地方式：
阅读资料不改变任何状态；只有 Step 的 Result 会改变状态。
所以界面上**不出现打勾、进度环、已读标记**。

---

## 6. 明确不做（v1 之外）

下面这些在实现之前那一版框架里已经写了或写了一半，v1 **全部砍掉**。砍掉的每一条都写清理由。
被砍掉的代码没有丢，都在 `legacy-v0/`（见第 7 节）。

| 砍掉的东西 | 理由 |
| --- | --- |
| **项目地图**（8 列关系图、连线、断裂检测） | 状态四块 + 问题列表已经能回答"我们现在做到哪了"。关系图是加分项，但它是目前最大的一块工程，也是你"不知道该从何下手"的主要来源 |
| **8 项状态板** → 收窄到 4 块 | Goal / 交付 / 问题 / 待确认 这四块来自你的描述；Scope、Decisions、Blockers、Next 在 v1 里没有对应的操作，留着只是空壳 |
| **四个直接动作** → 只留「下一步」 | "我卡住了""跑偏检查""问老师"都需要先有稳定的项目状态做输入。状态还没跑起来时，这三个动作没有东西可分析 |
| **用户任务（UserTask）节点** | 它解决的是"这个方法对应哪个使用场景"，属于深化问题，不是第一步 |
| **决策记录**（选择 / 为什么 / 依据 / 谁参与 / 是否确认） | 好机制，但 v1 的 Result 已经留下了"发现了什么、解决了哪个问题"，先让这条跑通 |
| **向老师提问整理器** | 需要"两种可能方向及其影响"才能生成，依赖前面的状态积累 |
| **跑偏检查的 8 类检测规则** | 规则依赖项目地图的节点与连线，地图不做，规则无处依附 |
| **全局搜索、文档全文检索** | v1 的资料推荐由步骤驱动，不需要学生自己搜 |
| **引用核验面板** | 证据块体系**保留并仍在使用**——推荐资料的页码定位、点开原文高亮都靠它。延后的只是"把所有引用集中到一个面板里逐条核验"这件事 |
| **测评与能力画像** | 上一轮已下线，v1 继续不做 |
| **多项目 / 教师后台 / 权限** | 一个项目、一个团队就够了 |
| **自动排期、甘特图、实时在线数据** | `项目文档.md` 本来就列为不做 |

---

## 7. 与现有代码的关系

现有代码不是白做的——**设计系统和资料库可以整体保留，页面层需要砍掉一半以上。**

### 直接保留（功能不变）

| 文件 | 用途 | 说明 |
| --- | --- | --- |
| `src/styles/tokens.css` `base.css` `element.css` | 设计系统（浅色 iOS 观感） | 原样沿用 |
| `src/components/AppIcon.vue` | 自绘图标 | 原样沿用 |
| `src/components/EmptyState.vue` | 空状态 | 原样沿用 |
| `src/components/DocViewer.vue` | 原文阅览，步骤详情里点开资料用 | 原样沿用 |
| `src/data/materials.ts` + `src/content/experience/*`（27 份）+ `src/utils/content.ts` | 课程资料库，降级为"推荐资料的来源" | 原样沿用 |
| `src/components/ConfidenceTag.vue` | 可信程度标记，正好是规则 2 的载体 | 只改了文案：`uncertain` 从「信息不足」改为**「待确认」**，与第 5 节的三档对齐；`origin` 为系统时不再重复显示「系统」 |
| `src/utils/markdown.ts` | markdown 渲染 | 只修了类型（markdown-it 15 的默认导出是构造函数，实例类型要单独引入），行为不变 |
| 工程骨架：Vite / TS / Pinia / Router / Element Plus | 构建与依赖 | 去掉 `tsconfig` 里已废弃的 `baseUrl`；`main.ts` 去掉未使用的语言包导入 |

### 改造

| 现有文件 | v1 怎么处理 |
| --- | --- |
| `views/CockpitView.vue` | 改造成**项目首页**：去掉 8 项状态板、4 个动作卡、断裂统计、约束区，改为 4 块状态 + 1~3 步 + 进展记录 |
| `views/KickoffView.vue` | 与**材料与理解**合并：从"学生凭空回答 7 问"改成"上传材料 → 系统整理 → 学生确认" |
| `components/UpdateFeed.vue` | 并入材料与理解页的确认流，保留"确认 / 修改 / 驳回"三态 |
| `data/thinking.ts` 的 `suggestNext` | **保留并成为核心**：输出从"Advice + 引用剩余周数"改成"Step + whyNow + questionId" |
| `components/KnowledgeCard.vue` | 简化为步骤详情里的"推荐资料"条目：去掉追问输入框，保留"为什么有用" |
| `types/project.ts` | 重写为第 4 节的 7 个对象 |
| `stores/viewer.ts` | 去掉对已删除的学习路径（`data/course.ts`）的依赖，改为直接持有 `docId` + `blockId` |
| `App.vue` | 去掉全局检索面板 |
| `TopBar.vue` | 从 4 个主入口收到 2 个，并把「待你处理」改为「待你确认」（数的是未经确认的系统推断） |

### 新增

| 文件 | 做什么 |
| --- | --- |
| `data/intake.ts` | 材料 → 四块草稿：四类关键词抽取；演示用的材料带预先整理好的结果（真实系统里这一步由模型完成） |
| `data/steps.ts` | 步骤生成引擎：第 4 节的四条来源 + 资料推荐与 `whyUseful` 拼装 |
| `views/StateView.vue` | 项目当前状态 |
| `views/MaterialsView.vue` | 材料与理解 |
| `views/StepView.vue` | 步骤详情 |
| `components/UnderstandingBlock.vue` | 四块状态共用：展示 + 确认 / 改一下 / 删掉 |
| `components/StepCard.vue` | 首页的步骤卡（`whyNow` 一直显示，不折叠） |
| `components/ResourceRow.vue` | 步骤详情页的推荐资料行（没有"已读"） |
| `utils/id.ts` | 会话内自增 id |

### 归档（移到 `legacy-v0/`，不参与构建）

| 现有文件 | 说明 |
| --- | --- |
| `views/MapView.vue` | 项目地图整页 |
| `views/LogView.vue` | 内容并入首页的「进展记录」区段 |
| `views/CockpitView.vue` | 8 项状态板 + 4 个动作，收窄成首页 |
| `views/KickoffView.vue` | 7 问启动快照，改成"先交材料、系统先读" |
| `components/ActionPanel.vue` + `components/actions/*`（4 个文件） | 四个直接动作，「下一步」改为首页内联 |
| `components/DecisionList.vue` | 决策记录延后 |
| `components/ThinkingQuestions.vue` | 追问清单是"我卡住了"的一部分，一并延后 |
| `components/UpdateFeed.vue` | 改为理解条目上的「确认 / 改一下 / 删掉」 |
| `components/KnowledgeCard.vue` | 简化为步骤详情页的推荐资料 |
| `components/SearchPalette.vue` | 全局检索，v1 的资料推荐由步骤驱动 |
| `data/thinking.ts` | 思考引擎（八类思考动作），思路重写为 `data/steps.ts` |
| `data/course.ts` + `stores/course.ts` | 学习路径与界面级状态，v1 没有对应页面 |

> **为什么是「归档」而不是「删除」**：这些文件当时**还没提交过 git**（`??` 状态），直接删会永久丢失，
> 所以整体移到了 `legacy-v0/`。它在 `src/` 之外，而 `tsconfig.json` 只包含 `src/**`，
> 因此不参与类型检查与构建。`legacy-v0/README.md` 里逐条写了砍掉每一份的理由。
> **确认 v1 没问题之后，这个目录可以整个删掉。**
>
> 例外是三份已在提交 `3daf799` 中的文件（`data/course.ts`、`stores/course.ts`、`components/SearchPalette.vue`），
> 以及旧 `types/project.ts` —— 它当时也没提交，被重写覆盖了。它定义过的类型
> （`NodeKind` / `NodeIssue` / `Decision` / `Escalation` / `Kickoff` / `StateBoard` / `ThinkingAction` 等）
> 现在只散落在 `legacy-v0/` 的各文件里，需要时从那些文件的用法反推。

---

## 8. v1 完成的标准

不看页面数量，只看这几件事能不能做到：

1. **材料能变成理解**：学生交进材料后，系统整理出的四块里，每一条都能指出它来自哪份材料的哪一部分。
2. **确认不费力**：学生能在 10 分钟内完成四块的确认或改写，不需要填任何复杂的表。
3. **下一步有理由**：系统给出的每一步都写清了「为什么现在做」，学生能对着这句话判断要不要现在做。
4. **问题和资料挂得住**：每一步都能看到它对应哪个问题，以及配套资料「为什么有用」——理由是用当前问题的说法写的，不是泛泛的"这一章很相关"。
5. **结果真的改变了状态**：学生完成一步、写下四个字段之后，首页的状态和进展记录确实变了 —— 不是只写进数据库没人看。
6. **没有任何未确认就生效的内容**：全程走一遍，找不到一处系统推断被当成项目事实。

**当前状态**：以上 6 条已逐条走通（浏览器实测，不是推断）。走通的路径是：
交材料 → 整理出 13 条草稿 → 全部确认 → 自动给出 3 步 → 完成第 1 步 → 首页状态与进展记录同步更新
→ 重新判断下一步，产出新的第 1 步。

尚未验证的是**第 4 条的前半句**——「学生能对着这句话判断要不要现在做」。这一条只能找真人试，见文末的风险说明。

---

## 9. 已定的 5 件事（原为「待你确认」，现已按推荐方案落地）

| # | 当时的问题 | 结论 | 落地情况 |
| --- | --- | --- | --- |
| 1 | 「上传材料」做到什么程度？ | **A：粘贴文本 + 预置演示材料** | 已做。上传 PDF / Word 并在浏览器里抽正文留到下一版 |
| 2 | 演示案例继续用「极端风浪复合事件」？ | **继续用** | 已做。3 份材料（需求对接会纪要 / ERA5 数据说明 / 第 3 周组会记录）+ 一步已完成的进展作为起点 |
| 3 | 三个动作（我卡住了 / 跑偏检查 / 问老师）延后？ | **延后到 v1.1** | 已归档到 `legacy-v0/` |
| 4 | 项目地图整个移除？ | **整个移除**，不做极简版 | 已归档。状态四块 + 问题列表承担了它的作用 |
| 5 | v1 只支持一个项目？ | **是** | 已做。不做项目列表、多团队切换、教师查看 |

第 1 条的取舍理由：先把「整理 → 确认 → 下一步」这条链路验证明白，材料输入方式随时可换，不构成返工。

---

## 附：v1.1 候选（现在不做，但记下来）

按"补上后能明显提升价值"排序：

1. **我卡住了**：学生说卡在哪，系统判断卡点类型（需求 / 数据 / 方法 / 结果解释 / 协作）并给出追问
2. **向老师提问整理器**：把已有工作 + 两种可能方向及其影响整理成一段可检查的问题
3. **决策记录**：记录关键选择及其理由，让汇报时能说清"为什么这么做"
4. **跑偏检查**：对照需求、问题、任务与证据，找出逻辑断掉的地方
5. **项目地图**：把问题、资料、证据、判断的关系画出来
6. **引用核验**：每条结论都能回溯到材料的具体页码并高亮

---

## 附：v1 最大的风险

**风险在于"1~3 个步骤"可能给不出学生真正认的下一步。**

如果系统给出的步骤只是"读一读第三章"这种程度，学生第二天就不会再打开它。
所以在实现时，`whyNow` 必须写实 —— 要能引用具体的东西（比如剩余周期、已确认的问题、
上一步留下的不确定），不能是空话。这一点建议在实现完之后**先找一两个学生试一次**，
看他们认不认这个"下一步"，再决定要不要继续往下做。