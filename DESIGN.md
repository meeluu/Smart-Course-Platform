# DESIGN.md · 工作台

界面与 `platform-ui-mockup(3).html` 保持一致，样式同源于 `src/styles/platform.css`。
改样式时请同步更新本文件。

## 世界

学生打开平台要立刻看清两件事：**项目现在走到哪个里程碑**、**下一步该做什么**。
因此有项目时首屏是一条五段里程碑流程条，其下是三栏工作台——
左边是项目事实（材料 / 里程碑 / 活跃度 / 疑问），中间是 AI 顾问的步骤建议，右边是提交证据。

层次**只由三样东西**建立：

1. **底色差**：页面 `#f5f6f8`，卡片纯白 `#ffffff`
2. **发丝线**：卡片边框 `#e3e2dd`，卡内分隔 `#efeee9`（列表项用虚线）
3. **字号与字重阶梯**：不靠颜色深浅制造层次

圆角统一 12px（卡片）/ 10px（卡片内小块）/ 8px（按钮与输入），标签用胶囊形 10px。

## 色彩

四支语义色，各自只承担一种状态，**不混用**：

| 语义 | 主色 | 深色文字 | 浅底 | 边框 | 用在哪 |
| --- | --- | --- | --- | --- | --- |
| 进行中 / 可点 | `#185FA5` | `#0C447C` | `#E6F1FB` | `#B5D4F4` | 主按钮、当前里程碑、负责人标签、地图中心 |
| 已完成 | `#1D9E75` | `#0F6E56` | `#E1F5EE` | `#9FE1CB` | 完成的里程碑、完成标志、提交按钮 |
| AI 生成 | `#534AB7` | `#3C3489` | `#EEEDFE` | `#CECBF6` | 步骤卡左侧竖线、AI 说明块、步骤序号 |
| 待解决 | `#FAC775` | `#854F0B` | `#FAEEDA` | `#FAC775` | 未解决的疑问、论文页提示条 |

进度条与流程条另有中间档：进行中 `#378ADD`，柱状图 `#85B7EB`。

中性四档，全部达到可读对比：正文 `#2c2c2a`、次级 `#444441`、说明 `#5f5e5a`、
弱化 `#888780`；分隔与占位用 `#d3d1c7` / `#f1efe8` / `#f8f7f4`。

## 字体

`Microsoft YaHei` → `PingFang SC`，正文 14px。
卡片标题 14px/600，说明文字 12.5px，最小字号 11px（标签与时间戳）。

## 布局

- 顶部标题栏高 56px，`sticky` 置顶；其下导航条 `sticky top:56px`，当前页签用蓝色下边框标记。
- 内容区 `max-width:1440px` 居中，左右各 24px 内边距。
- 工作台三栏：`280px | 1fr | 320px`，间距 16px；窗口小于 1100px 时收成单列。
- 卡片间距 14px，卡片标题栏与内容用发丝线分隔。

## 项目地图

左侧五个里程碑卡片，中间项目本体（实心蓝底），右侧 AI 正在追踪的当前步骤（紫底）。
连线用贝塞尔曲线，颜色跟随源端状态；顶部标注「里程碑」与「当前实际步骤」。
配色映射：绿=已完成、蓝=进行中、灰=未开始。

## 拒绝的做法

- 不用渐变承担层次（唯一的渐变是 AI 说明块的浅紫→浅蓝，用于标记"这是 AI 说的"）
- 不用投影：卡片靠边框与底色差浮起
- 不用图标字体与 emoji 当结构（表单里两个上传框的 📄 / 🗂 是唯一的例外，便于一眼区分手册与数据）
- 状态不用颜色深浅区分**程度**，只区分**种类**
