# DESIGN.md · 「深海读数」设计系统

本文件记录已落地的视觉决策。改动 `src/styles/tokens.css` 时请同步更新本文件。

## 世界

课程的证据链是界面唯一的主角；只有「正在发生」的事物才允许发光。

- 使用场景：学生在实验室或宿舍、室内照明下连续数小时研读课件、跑实验、核验规范。
  因此底色是有色的深海水体而非纯黑，正文靠高对比冷白承载长时阅读。
- 高度只声明一次：玻璃面板用 1px 镜面发丝线，**不使用投影**。
- 玻璃与模糊只用于内容确实从其下滚过的位置：顶部任务栏、左侧导航轨、右侧原文面板，其余面板只用半透明填充 + 描边。

## 色彩（Committed：一族强调色承担全场）

水体由深到浅：`--void #04080f` → `--abyss #060c17` → `--substrate #0a1322`。
强调色只有青蓝一族：`--cyan #35e0f0` → `--azure #4aa8ff` → `--indigo #6d7bff`，
渐变令牌 `--grad-accent` 只用于品牌标识与主行动按钮。

文本全部带蓝相，绝不使用纯灰；对比度实测：`--ink-mute #7d94b0` 6.2:1，
`--ink-faint #6b82a0` 4.8:1，`--ink #e8f1fb` 约 16:1。青蓝底上文字统一用
`--ink-on-accent #04202b`。语义色降饱和以服从冷色场：`--ok #43d9a3`、
`--warn #f2b455`、`--risk #ff7a8a`。

## 字体

- 显示体：拉丁与数字走 Sora（自托管可变字体 `@fontsource-variable/sora`），中文回退系统黑体。
  中文字体族必须排在 `ui-sans-serif` / `system-ui` 之前，否则 Windows 上会回退成宋体，
  标题与正文的重量脱节。
- 正文：中文优先的系统黑体栈。等宽只用于代码、页码与字面量。
- 标题字距 -0.022em；正文行高 1.7，最大行宽 74~76ch。

## 尺度与动效

- 圆角 6 / 8 / 12 / 16，胶囊仅用于小控件；间距 4~96 共九级，标题上方恒大于下方。
- 曲线统一 `cubic-bezier(.16,1,.3,1)`，时长 140 / 240 / 420 / 900ms。
- 全局只有两处刻意动效：路由切换的淡入上移、课程概览里学习闭环连接线的一次性绘制。
  其余过渡只服务于状态变化。

## 构件规则

- **资源行而非卡片网格**：学习路径上的资源用行组呈现（类型图标 + 标题 + 页码 + 锚点），
  保证信息密度；不使用同尺寸图标卡片铺满页面。
- **图标**：`components/AppIcon.vue` 自绘，统一 24×24 视框与 1.6 描边，禁用图标字体与 emoji。
- **引用卡片**：编号徽标 + 文档名 + 章节 + 页码 + 类型 + 摘录 + "核验原文"，
  这是全平台的核心交互，任何新视图涉及结论时都应复用。
- **证据不足**：不使用"换个问法试试"这类空话，必须说明检索范围与缺口。

## 明确不用

渐变文字；标题上方的眉标/小标签；01/02/03 式的段落编号（除非顺序本身携带信息）；
纯装饰性的毛玻璃；彩色左边框；硬偏移投影；零偏移彩色光晕；进度环与火花线代替真实内容；
emoji 充当图标；用等宽字体营造"技术感"。
