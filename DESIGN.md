# DESIGN.md · 「纸上系统」

参考 iOS / macOS 原生观感。改动 `src/styles/tokens.css` 时请同步更新本文件。

## 世界

界面唯一的主角是「项目当前状态」。它必须看起来像一张可以安静读下去的纸，
而不是一块会发光的控制台。因此层次**只由三样东西**建立：

1. **底色差**：页面 `#f2f2f7`，卡片纯白 `#ffffff`，卡内次级面 `#f7f7fa`
2. **发丝分隔线**：`rgba(60,60,67,.14)`，列表内从左侧内缩 16px
3. **字号与字重阶梯**：不靠颜色深浅制造层次

明确拒绝（上一版的做法，已全部移除）：深海底色、彩色发光、渐变强调、
毛玻璃满屏铺、大写字距科技标签。

## 色彩

页面 `--page #f2f2f7`，卡片 `--surface #ffffff`，卡内次级面 `--surface-2 #f7f7fa`；
填充 `--fill rgba(120,120,128,.08)`，分隔线 `--separator rgba(60,60,67,.14)`。

强调色只有一支：系统蓝 `--accent #007aff`，只用于「可点 / 已选中 / 正在发生」。
正文字号下的蓝字改用 `--accent-ink #0059c7`（6.4:1）。

中性文本四档，全部达标：`#1c1c1e` 17:1 / `#3a3a3c` 11:1 / `#55555a` 7.0:1 /
`#6e6e73` 5.2:1。语义色统一换成可读的深色档：`#248a3d` / `#c93400` / `#d70015`。

## 字体

全站系统字体栈：`-apple-system` → `SF Pro` → `Segoe UI` → `苹方` → 微软雅黑。
不再加载任何外部字体。标题字距 -0.014em，正文 15px / 行高 1.6 / 行宽 72ch。

## 动效

曲线统一 `cubic-bezier(0.32, 0.72, 0, 1)`，时长 140 / 220 / 320 / 480ms。
丝滑来自**短距离 + 快出缓停**，不是长时长：路由切换位移只有 6px，
按钮按下 `scale(0.98)`，抽屉从右侧 24px 滑入。全局没有滚动视差。

## 构件规则

- **卡片是白面浮在浅灰底上**，靠底色差分层；卡片不加描边、不加投影。
- **列表内分隔用 1px 发丝线**，不是每一行都做成卡片。
- **投影只留给真正浮起来的层**：动作面板（`--shadow-2`）、下拉浮层。
- **图标**：`components/AppIcon.vue` 自绘，24×24 视框、1.6 描边，
  禁用图标字体与 emoji。
- **顶栏是 iOS 材质**：半透明白 + `blur(20px) saturate(180%)`，
  这是唯一允许使用毛玻璃的位置。

## 交互层的视觉约束（不可协商）

视觉替产品守住三条红线。改动前请先读 `README.md` 的「三条硬约束」：

1. **没有一键采纳**。AI 产出的旁边不出现「采用」类单一按钮；判断控件一律是三选一
   （接受 / 部分接受 / 拒绝）加一个必填理由，理由为空时提交为禁用态。
2. **建议之前先问判断**。给建议的视图必须先出现「你们的判断」区块，再出现建议列表；
   顺序不能调换，也不能折叠。
3. **可信度必须可见**。系统推断的内容都带 `ConfidenceTag`，且不得用更浅的文字色淡化它。
4. **断裂用虚线**。地图里出问题的连线用 `stroke-dasharray: 4 4` 并转成 `--risk`；
   虚线在全站只表示「这条链路没接上」。

## 明确不用

渐变（任何形式的渐变背景与渐变文字）；彩色发光与零偏移光晕；
大写字距的科技标签；深色底；满屏毛玻璃；彩色左边框；
纯装饰的进度环与火花线；emoji 充当图标；用等宽字体营造"技术感"。
