# 项目交接文档

> **这份文档写给谁**：接下来负责「开发计划与设计」的那位（人或 AI）。你手上没有上下文，这份文档要让你不用重新翻代码就能做计划。
>
> **它不是计划书**。它只描述「现在是什么样」和「哪里还缺」，不规定该怎么做。
>
> **先核实再动手**：文末第 11 节给了三条验证命令，10 分钟能跑完。别只信这份文档。

---

## 0. 三十秒版

一个给**正在做课程项目的大学生小组**用的推进平台。

学生选定课程题目、上传实验手册和数据集说明后，**AI 项目顾问**按项目当前阶段给出 **1~3 步建议**（每步写清「为什么现在做」和「完成标志」）；学生认领步骤、做完后**提交证据**，平台据此更新里程碑状态，再给出下一步。

两个页签：**工作台**（创建项目 + 三栏看板）、**论文推荐**（① 给可核验的论文入口 ② 给检索提示词）。

**当前状态**：界面与交互已按设计稿实现完，跑得起来、点得通。但**数据全在内存里，刷新即重置，没有后端**。它是一个「能演示、能验证」的原型，不是能交给学生日常用的产品。

---

## 1. 这个项目是什么

### 1.1 定位

帮**做项目的小组**回答两个问题：**我们现在走到哪了**、**下一步该做什么**。

立场很明确：**AI 不给答案**。它给的是「下一步该做什么、为什么现在做、做完算什么样」，以及「去哪查文献的提示词」。

### 1.2 用户与场景

- 用户：选了这门课、正在做项目的本科生小组（3~5 人）
- 场景：课程周期 16 周，学生从选题开始，到中期检查、最终演示与论文
- 他们现在的痛点：**不知道该先做什么**、文献不会查、组内分工不清、做了很多但说不清推进了什么

### 1.3 一句话边界

| 是 | 不是 |
| --- | --- |
| 项目推进工具（状态 + 下一步 + 证据） | 课程资源库 / 题库 |
| 给**步骤**和**理由**；给可核验的论文入口（真实 DOI 或检索提示词） | 给答案、给代码、给模型编出来的文献 |
| 过程留痕（谁做了什么、留下什么） | 打分系统 / 排行榜 / 能力画像 |

---

## 2. 课程约束（这些是硬约束，不是产品偏好）

| 约束 | 内容 | 影响 |
| --- | --- | --- |
| 课程 | 《大数据分析实践》，16 周 | 学生只有 16 周，平台必须轻 |
| 团队 | 3~5 人小组，必须能看出组内分工 | 步骤要带「建议负责人」 |
| 交付 | 可交互原型 + 论文 + 演示 | 演示时不能崩、要能现场点 |
| 数据工作坊 | 老师要求准备：项目 Question、可 1-2 分钟打开的数据子集、数据说明、最困扰的数据问题 | 已交付，见 `9.21周一大数据分析实践课程需要的数据/` |
| 课程题目 | 9 个真实课题（海洋方向为主） | 已内置为模板，见 `src/data/topics.ts` |

**关键事实**：9 套题目模板里的内容（里程碑、疑问、3 步建议、论文检索方向）**是真实且已经过设计的内容**，不是占位符。它们是这个平台的核心资产——决定产品好不好的不是界面，是这些建议的质量。做计划时不要把这块当成 demo 数据。

---

## 3. 当前状态

### 3.1 能跑通的

| 能力 | 状态 |
| --- | --- |
| 创建项目（从 9 个题目选，或自定义；填人数；选传实验手册/数据集） | ✅ 可用 |
| 多项目创建与切换（左栏下拉） | ✅ 可用 |
| 五段里程碑流程条 + 里程碑进度列表 | ✅ 可用 |
| AI 项目顾问：3 步建议（标题 / 建议负责人 / 为什么现在做 / 完成标志） | ✅ 可用 |
| 「认领这一步」→ 当前里程碑进度 +25%（上限 90%） | ✅ 可用 |
| 「就此提问」→ 自动往对话框追加一问一答 | ✅ 可用 |
| 「相关论文」→ 跳到论文推荐页 | ✅ 可用 |
| 项目地图（SVG：里程碑 → 项目 → 当前步骤） | ✅ 可用 |
| 向顾问提问（答疑模式，回复池轮换） | ✅ 可用 |
| 提交步骤证据（四字段 + 附件）→ 写时间线、并入疑问、活跃度 +1 | ✅ 可用 |
| 未解决的疑问 + 「标记已解决」 | ✅ 可用 |
| 近 4 周活跃度柱状图 | ✅ 可用 |
| 论文推荐：**① 直接打开**（有 DOI 的开原文，没有的按标题检索）**② 复制检索提示词** | ✅ 可用 |
| 论文页可现场生成专属检索方向（按项目当前阶段拼装提示词） | ✅ 可用 |
| 全局提示条（toast） | ✅ 可用 |

### 3.2 做不到的（重要）

| 缺口 | 后果 |
| --- | --- |
| **没有持久化** | 刷新页面全部重置。这是当前最大的问题 |
| **没有后端** | 纯前端，无法多人协作、无法真正记住谁做了什么 |
| **AI 是假的** | 「AI 项目顾问」的回复是写死的回复池 + 题目模板里预置的步骤。没有接任何模型 |
| **锚点不够实** | 步骤建议不会随项目实际状态变化——三份证据提交上去，建议还是那三条 |
| **没有账号** | 多人协作、组内贡献度都无从谈起 |
| **附件只存文件名** | 选了文件只是把名字记下来，文件本身没被读取或保存 |

**第 3 行和第 4 行是产品层面的核心缺口**：现在这套「AI 顾问」本质是**静态内容展示**，真正的产品价值（根据学生实际情况给出下一步）还没实现。

---

## 4. 技术栈

### 4.1 实际在用的（就这 5 个）

| 层 | 选型 | 版本 | 说明 |
| --- | --- | --- | --- |
| 框架 | Vue | ^3.5.42 | 全部 `<script setup lang="ts">` 组合式 API |
| 构建 | Vite | ^8.3.0 | dev server + 生产构建 |
| 路由 | vue-router | ^5.3.1 | hash 模式，2 条路由懒加载 |
| 状态 | pinia | ^4.0.3 | 只有 1 个 store |
| 语言 | TypeScript | ^6.0.3 | `strict` + `noUnusedLocals` + `noUnusedParameters` |

样式：一份全局 CSS（`src/styles/platform.css`），**没有 CSS 框架、没有预处理器**。
图表：项目地图是**手写 SVG 字符串**，没有引图表库。

### 4.2 开发工具链

`vue-tsc`（类型检查）、`ESLint 10 + eslint-plugin-vue + typescript-eslint`、`Prettier`、npm。

脚本：`dev` / `build` / `preview` / `typecheck` / `lint` / `format`。

### 4.3 依赖已精简

运行时依赖**只有 3 个**（`vue` / `vue-router` / `pinia`），devDependencies 10 个。`npm run build` 约 0.3 秒。

已清掉的 7 个：`element-plus`、`@element-plus/icons-vue`、`markdown-it`、`highlight.js`、`katex`、`dompurify`、`@types/markdown-it`
—— 上一版组件库与课程资料管线用到的，界面重构后零引用，已随管线一起删除（见 6.2）。

`public/images/`（22 张图，9.9 MB）已从版本控制移除并加入 `.gitignore`，**文件仍在本地**。

---

## 5. 仓库与交付物

### 5.1 Git 状态

| 项 | 值 |
| --- | --- |
| 远端 | `https://github.com/meeluu/Smart-Course-Platform`（**公开仓库**） |
| 默认分支 | `main` —— **受保护分支，只能通过 Pull Request 修改** |
| 当前分支 | `feature/vue3-platform-rebuild`（已推送，已设上游跟踪） |
| 最新提交 | `db109e7` 界面按设计稿重构为「工作台 + 论文推荐」两页签 |
| 历史提交 | `3daf799` Vue3 重建 → `05c5eb1` 收窄到 v1 单闭环 → `db109e7` 当前界面 |
| 待合并 | **PR #1**（`feature/vue3-platform-rebuild` → `main`），状态 Open，**尚无评审** |

**注意**：`origin/main` 目前**只有最初的脚手架模板**。所有工作都在功能分支上，要进 `main` 必须先合并 PR #1（需要协作者 Approve —— 开 PR 的人不能 approve 自己的 PR）。

### 5.2 不进版本库的东西（`.gitignore`）

仓库是**公开**的，所以下面这些只在本地：

```gitignore
会议纪要.md                    # 真实会议记录
老师与gpt的对话.md              # 与老师的沟通记录
项目文档.md / *.docx            # 需求文档
9.21周一大数据分析实践课程需要的数据/   # 课程交付物（含会议纪要副本）
*.zip
legacy-v0/  legacy-v1/         # 旧版代码归档（见 7）
platform-ui-mockup*.html       # 设计稿
public/images/                 # 未使用的图片
```

**接手时请遵守这条**：不要把上述任何内容提交上去，也不要在公开的代码/注释里写真实机构名与会议内容。

### 5.3 课程交付物（本地，9/21 已交）

`9.21周一大数据分析实践课程需要的数据/` 对应老师要的四项：

| 文件 | 内容 |
| --- | --- |
| `README.md` | 导航 |
| `①课程资源数据样本/corpus.csv` | 145 个可引用片段（往届材料 102 + 课程文档 43），12 个字段 |
| `①课程资源数据样本/字段说明.md` | 字段含义 + 片段怎么切出来的 |
| `②真实学生项目/项目概况.md` | 一个真实学生项目：Question / 材料 / 当前进展 / 6 个待确认问题 |
| `③数据现状与缺口/数据现状与可能需要的数据.md` | 已有 7 项数据、还缺 8 项、课程资源与项目问题如何关联 |
| `③数据现状与缺口/eval-set.csv` | 32 条人工标注（正 15 / 负 17） |
| `③数据现状与缺口/评估脚本/measure.mjs` | 量化规则抽取的脚本 |

---

## 6. 代码结构

### 6.1 当前应用（约 1730 行，这是全部有效代码）

```
src/
├── main.ts                          9 行   入口：Pinia + Router + platform.css
├── App.vue                         39 行   标题栏 + 两页签 + toast
├── router/index.ts                 17 行   2 条路由 + 兜底重定向
├── views/
│   ├── WorkbenchView.vue          344 行   工作台：流程条 + 三栏（最重要）
│   └── PapersView.vue              70 行   论文推荐
├── components/
│   ├── ProjectCreate.vue           81 行   创建项目表单
│   ├── ProjectMindMap.vue          79 行   项目地图（SVG 转义）
│   └── PaperDirectionCard.vue      54 行   论文检索方向卡（含复制逻辑）
├── stores/workbench.ts            302 行   唯一的 store，全项目状态
├── data/topics.ts                 525 行   9 套题目模板（核心内容资产）
├── types/platform.ts               89 行   领域类型
└── styles/platform.css            117 行   全部界面样式（与设计稿同源）
```

### 6.2 已删除的部分（上一版的课程资料管线）

上一版有一整套「课程资料推荐」管线，约 1590 行。因为产品改为**推荐论文**（链接 + 检索提示词），
这套东西已全部删除：

```
src/data/materials.ts                   课程证据块（课件/实验手册/规范/评分/FAQ）与往届材料登记
src/utils/content.ts                    正文装载、按标题切块、检索
src/utils/markdown.ts                   markdown 渲染（KaTeX / 代码高亮 / 锚点）
src/types/course.ts                     证据块与资料文档类型
src/styles/{base,element,tokens}.css    上一版样式系统（未被 import）
src/content/experience/*.md             27 份往届学生项目材料，94 KB
```

除 `src/content/experience/` 被移到 `legacy-v1/`（系统删除保护挡住了递归删除，见第 7 节）外，其余已从仓库删除。
需要取回时用 `git show db109e7:<路径>`。

---

## 7. 被归档的东西（决策历史，避免重复讨论）

| 目录 | 是什么 | 为什么归档 |
| --- | --- | --- |
| `legacy-v0/`（18 文件） | 最早那版：项目地图 + 证据与决定 + 项目启动 + 四个直接动作（我卡住了/跑偏检查/下一步/问老师） | 范围过大 |
| `legacy-v1/`（21 文件） | 上一版 v1：**材料 → 读出四块（项目目标/交付内容/当前问题/待确认事项）→ 学生逐条确认 → 给 1~3 步 → 提交结果与证据**，带「未确认不进项目状态」的门禁 | 界面被设计稿替换；但**它的产品逻辑比现在这版完整** |

取回方式：`git show 05c5eb1:src/views/StateView.vue`（v1 的文件就在这个提交的 `src/` 下）。
旧的 `legacy-v0/` 内容在同一个提交里。

`legacy-v1/src/content/experience/` 里的 27 份往届学生项目材料，是 2026-09-23 移进去的
——系统删除保护挡住了递归删除，所以改为归档。它已随 `legacy-v1/` 一起被 `.gitignore` 排除。
其余被删掉的课程资料管线代码见 6.2。

**这是做计划时必须先回答的问题**：v1 的「四块状态 + 确认门禁」是这套产品最扎实的部分（AI 读材料 → 学生确认 → 才成为事实），而现在的界面里**它不存在**。是：
- (a) 把它作为第三个页签加回来？
- (b) 把它的产物（四块状态）并进工作台左栏「项目资料」下方？
- (c) 放弃它？

---

## 8. 数据模型

全部定义在 `src/types/platform.ts`，只有一个 store 持有。

```
Project
├── name            项目题目（完整名）
├── short           短名（地图中心、标签用）
├── group           第 N 组
├── members         人数
├── updated         最近更新的一句话
├── ms: Milestone[]  5 个里程碑  { t, s: 'done'|'cur'|'todo', p: 0-100, sub }
├── doubts: string[]          未解决的疑问
├── banner           AI 顾问的开场说明（每个题目不同）
├── steps: SuggestedStep[]    3 步建议 { t, owner, why, done }
├── papers: PaperDirection[]  3 条论文检索方向 { title, meta, why, prompt }
├── evidence: EvidenceItem[]  证据时间线 { time, text, who }
├── weekly: number[4]         近 4 周活跃度计数
├── materials: MaterialItem[] { name, type: '实验手册'|'数据集'|'材料' }
├── chat: ChatMessage[]       对话 { who, text, me }
└── aiPapers: AiPaperDirection[]  现场生成的检索方向 { title, meta, prompt }
```

**注意 `weekly` 初值是 `[0,0,0,0]`**（与设计稿一致），所以新项目「近 4 周活跃度」是空的——这是设计稿原样，不是 bug。

---

## 9. 交互闭环（当前实际发生的）

```
创建项目（选题目 + 人数 + 选传材料）
        ↓
左栏出现：项目资料 / 5 个里程碑（第 1 个「进行中 10%」）/ 活跃度 / 疑问
中栏出现：AI 顾问开场说明 + 3 步建议
        ↓
点「认领这一步」→ 当前里程碑进度 +25%（上限 90%）
点「就此提问」→ 对话框追加一问一答
点「相关论文」→ 跳论文推荐页
        ↓
右栏填写证据（完成了什么 / 发现了什么 / 解决了哪个问题 / 还有什么不确定 + 附件）
        ↓
提交 → 时间线插入一条 / 「还有什么不确定」进入左栏疑问 / 本周活跃度 +1
```

**闭环是断的**：提交证据之后，中栏的 3 步建议**不会变**。真正的产品应该是「证据 → 更新状态 → 重新给建议」。

---

## 10. 下一步要做的功能

### 10.1 已明确的需求（用户 2026-09-23 提出）

**需求 A：项目计划表 + 可视化**

平台里要能看见项目的整体计划，并且把它可视化。拆成三条：

1. **一个整体的计划表** —— 行是**阶段 / 里程碑**，列出**关键任务、建议负责人、状态、进度**
2. **目前重要的计划** —— 从计划里挑出当前阶段最该做的 2~3 条，**单独突出**，不要埋在大表里
3. **计划表可视化** —— 按阶段展示推进情况：每个里程碑一条，条状表示状态与进度，**当前阶段要有标记**

> **不要求按周展开时间表**（16 周逐周排期不做）。计划表的行是阶段与里程碑，不是周。

**这三条都需要计划设计者先拍板下面几个问题：**

| 问题 | 选项 |
| --- | --- |
| 计划数据从哪来？ | (a) 给 9 套题目模板各加一份计划数据（`plan[]`，5 行 × 关键任务）—— 质量最高，但要写 45 行内容<br>(b) 从现有里程碑推导（任务取里程碑名 + 通用任务）—— 零新增数据，但内容泛<br>(c) 混合：里程碑 1 用模板已有的 3 条 steps（题目专属），其余用通用任务 |
| 放在哪？ | 第三个页签「项目计划」，还是工作台里加一张卡？（工作台三栏已经很满，再塞会挤） |
| 会不会重复？ | 两处都可能撞车：①「目前重要的计划」与已有的「AI 项目顾问 3 步建议」；②**按阶段的可视化与已有的「五段里程碑流程条」「项目地图」高度重叠**——不做时间轴之后，这三者画的几乎是同一件事，需要先想清楚各自的分工 |

**需求 B：论文推荐支持两种方式**（**已实现**）

① **直接给链接** —— 能定位到具体论文的，用 DOI 链接点开即读原文；没能定位的，退化为「按标题检索」的链接
② **给提示词** —— 复制到 GPT 自己检索

实现方式：`PaperDirection` 增加了可选的 `link` 字段（`src/types/platform.ts`），
`PaperDirectionCard.vue` 同时渲染两个按钮并在卡片上标明用的是哪一种。

**关于缺链接（2026-09-23 改方案，见需求 C）**：9 套模板共 27 条推荐里只有 **2 条**
填了真实 DOI。原计划是逐条人工补，现已改为**接入后端后由检索 API 提供**——
不要再去手工填不确定的 DOI（填错会 404）。在接上之前，缺链接的条目继续走「按标题检索」。

**需求 C：论文推荐改由后端生成**（用户 2026-09-23 定）

**背景**：27 条预置推荐里只有 2 条有真实链接。已决定不再逐条人工补 DOI，
改由「检索 API + 大模型」生成。**前提：大模型 API key 只放后端**（已确认）。

**分工（不可互换）**

| 谁 | 产出 |
| --- | --- |
| 后端 → OpenAlex / Crossref（免费、不需要 key） | 真实论文的标题、DOI、链接 |
| 大模型 | 从检索结果里挑 3 条，并写「为什么这几篇对你们有用」 |
| 大模型 | ② 检索提示词（`prompt`），结合项目当前阶段生成 |

> **① 的链接必须来自检索 API，不能由模型生成**——模型会编出不存在的论文与 DOI，
> 编号格式看着对、点开 404。而「直接打开」这条路上学生一定会点。
> 模型在这件事上只负责「挑」和「写理由」，不负责「给出处」。

**接口约定（前端已按此预留）**

| 接口 | 入参 | 返回 |
| --- | --- | --- |
| `POST /api/papers` | `{ projectId, projectName, currentMilestone, topic }` | `[{ title, doi, url, why }]` |
| `POST /api/papers/prompt` | `{ projectId, projectName, currentMilestone, ask }` | `{ prompt }` |

约定：

- **key 只放后端**，前端不持有，也不要走前端直连
- 前端唯一的出口是 **`src/services/papers.ts`**（2026-09-23 已抽出）：
  现在返回模板预置数据，接上后端后**只改这一个文件**，`PapersView.vue` 与
  `PaperDirectionCard.vue` 不用动
- 建议后端对同一 `projectId + currentMilestone` 的结果做缓存，
  否则每进一次论文页都要调一次模型
- 检索结果为空或超时时要能降级：返回空数组，前端不报错、页面不空白

**27 条预置推荐的处置**

| 部分 | 处置 |
| --- | --- |
| 25 条只有标题 + 提示词的 | 接入后端后**删除** |
| **2 条已核验 DOI**（Kroodsma 2018 Science、pocean 2015） | **保留**，作为接口不可用时的兜底 |
| 其中出现过的领域具体名词 | **写进模型的系统提示词**，要求它给出同等级别的具体方法名（见下） |
| `platform-ui-mockup(3).html` 里内嵌的那份副本（30 条 `prompt`、0 条链接） | **不动**。它是设计稿、不参与构建。注意它**不是真源**——以后改论文数据只改 `src/data/topics.ts` |

那句系统提示词要举得出例子，否则模型会退回泛泛的「检索该方向近年综述」：

```
DINEOF（叶绿素重建）、DARPA SUBOFF（救生艇几何与验证基准）、
SHAP / interpretable ML（捕捞努力量解释）、k-ω SST 与 y+ 控制（湍流模型）、
Global Fishing Watch / AIS 状态判别（捕捞努力量数据）
```

---

### 10.2 其他缺口与技术债（按我判断的优先级）

| # | 缺口 | 为什么重要 | 大致工作量 |
| --- | --- | --- | --- |
| 1 | **建议不会随状态变化** | 这是产品的核心价值。现在 3 步是静态的，学生交完证据看到的还是同样 3 步 | 中～大（要么规则引擎，要么接 LLM） |
| 2 | **没有持久化** | 演示时刷新一下就没了，不敢现场操作 | 小（localStorage 可先顶上）；中（真后端） |
| 3 | **AI 是假的** | 对话是回复池轮换，问什么都答那 4 句 | 中（接模型 + 提示词设计） |
| 4 | **多组协作不存在** | 组员各自看到的是各自浏览器里的东西 | 大（账号 + 后端 + 实时） |
| 5 | 附件只记文件名 | 提交证据不能真附东西 | 小～中 |
| 6 | 疑问没有「来源」 | 不知道为什么产生这条疑问 | 小 |
| 7 | 里程碑进度只能 +25% | 「认领」和「完成」没区分开 | 小 |
| 8 | 没有「完成一步」的概念 | 只有认领，没有确认完成 | 小 |
| 9 | 计划表还没有（见 10.1 需求 A） | 用户已明确提出 | 见 10.1 的选项 |
| 10 | 论文的真实链接只有 2 条 | 方式 ① 大多退化成了按标题检索 | 已改为后端检索（10.1 需求 C），不再人工补 DOI |

---

## 11. 怎么自己核实（别只信本文档）

```bash
npm install
npm run typecheck   # 应该无输出
npm run build       # 应该看到 "✓ built in ..."
npm run dev         # 打开 http://localhost:5173/
```

需要 Node **20.19+ 或 22.12+**（Vite 8 要求）。

然后在浏览器里走一遍，五件事：

1. 创建页选一个题目（推荐「《大数据分析实践》智慧课程平台」或「面向海洋环境安全保障的极端风和浪事件的智能分析系统」）→ 点「创建项目，让 AI 开始追踪」
2. 看工作台三栏是否齐了，**项目地图 SVG 是否画出来**（这是最容易坏的一块）
3. 点第一步的「认领这一步」→ 看左栏里程碑进度是否变了
4. 右栏填「完成了什么」→ 提交 → 看右栏时间线是否多一条、左栏疑问是否多一条
5. 刷新页面 → **你会看到所有数据都没了**（这就是缺口 #2）

另外可核对 git：

```bash
git log --oneline -3          # 应看到 db109e7 / 05c5eb1 / 3daf799
git status                    # 应该干净
ls legacy-v0 legacy-v1        # 两个归档目录应存在
```

---

## 12. 已经定下来的决定（不要重新讨论）

| 决定 | 理由 |
| --- | --- |
| **不给答案，只给步骤和理由** | 课程立场。AI 给答案就变成了代做 |
| **论文同时给「① 链接」和「② 提示词」** | **2026-09-23 更正**：原文是「只给检索提示词，不给链接」，与实现和后来的决定都不符。老师要练检索能力（②必须留）；但学生拿不到能直接读的论文就没法推进（①也要有）。**① 的链接只能来自检索 API（OpenAlex / Crossref），不能让模型生成**——见 10.1 需求 C |
| **预置的 27 条论文方向，接入后端后移除** | 不再逐条人工补 DOI，改由「后端检索真实论文 + 模型挑选并写理由」生成。保留 2 条已核验 DOI 作兜底，其中的领域具体名词写进模型的系统提示词。见 10.1 需求 C |
| **9 个题目用模板给出起点，但学生仍要输入自己的内容** | 题目本身不给自由输入——建议质量来自预先设计的模板，凭空写的题目就没有建议可给。但模板只是**起点**，不是「不用输入」：每组仍要填人数、交实验手册与数据集说明，之后每走一步都要提交证据、答疑。模板负责「什么建议是有道理的」，学生的输入负责「我们现在实际做到哪了」 |
| **样式照设计稿 1:1，不自创** | 设计稿是已确认的视觉基准（`platform-ui-mockup(3).html`） |
| **项目地图用手写 SVG，不引图表库** | 结构固定（里程碑/项目/步骤三层），引库不划算 |
| **仓库是公开的，课程素材一律不进库** | 会议记录、需求文档、教师信息都不能公开 |
| **`main` 受保护，只能走 PR** | 课程要求练习 PR 流程，不是我加的约束 |

---

## 13. 需要拍板的问题（做计划前请先问清楚）

1. **计划表怎么做？**（见 10.1 需求 A 里的三个问题）——**最新提出的需求，优先回答**
2. **v1 的「四块状态 + 确认门禁」怎么办？**（见第 7 节，三选一）——最影响计划结构
3. **建议要真的动起来吗？** 即：投入做「证据 → 重新给建议」这条闭环，还是先把它当演示原型打磨？
   这决定计划是「做功能」还是「做 AI」。
4. **要不要后端？** —— **已定：要**（2026-09-23）。论文推荐已确定走后端接口（10.1 需求 C），
   大模型 key 也只能放后端。剩下的只是「先做最小中间层，还是直接上完整后端」
5. **接口是接大模型还是继续规则？** —— **已定：接大模型，key 放后端**。
   规则只保留一段：论文①的链接由 OpenAlex / Crossref 检索得到。
   仍需想清楚的是「哪些环节接模型、系统提示词谁写」，见 10.1 需求 C
6. **交付时间点与验收方式是什么？** 中期检查 / 最终演示分别是什么时候、要看什么
7. **团队分工**：几个人、谁负责哪块、有没有人只做文档与汇报

---

## 14. 给计划设计者的建议

**先做的不是功能，是把「建议会不会动」这件事想清楚。**

理由：现在这套界面已经足够好看，也能演示。但如果 AI 顾问的建议永远是静态的三条，那这个产品在答辩时会被问一句话就答不上来——**「学生交完证据之后，你凭什么说下一步该做什么？」**

这句话决定整个技术路线：

- 若答案是「靠规则」→ 需要设计一套从「里程碑状态 + 证据 + 疑问」推导下一步的规则，工作量可控，但要设计规则
- 若答案是「靠模型」→ 需要设计提示词、结构化输出、以及 key 的存放方案，工作量更大，但更接近产品本意

**关于最新提出的「计划表」（10.1 需求 A）**：它独立于下面的顺序，可以并行做。但它会占掉界面空间，
所以先定「放在哪、计划数据从哪来」这两件事，再动手写代码——否则很容易做出一张没人看的表。

**关于「论文推荐接后端」（10.1 需求 C）**：它也独立于下面的顺序，可以并行做，但它带一个前提——
**得先有一个最小后端**（转发大模型与检索 API，顺便把 key 藏起来）。
所以「要不要后端」已经不用讨论了，只剩「最小中间层还是完整后端」。
另外：这个最小后端一旦有了，下面第 1 条的**持久化**可以一并解决。

**建议的计划顺序**（仅供参考，你可以推翻）：

1. 先补**持久化**（哪怕只是 localStorage）——否则连演示都不敢点
2. 再让**证据驱动状态**（至少：提交证据后里程碑推进、疑问关联到步骤、建议里出现「你们上一步留下了 X」）
3. **计划表 + 可视化**（需求 A）——先把「目前重要的计划」这一条做出来，它比整张大表更有用
4. 然后决定**建议生成**走规则还是模型，做一条最小可用链路
5. 最后才是多组协作与账号（**后端本身要提前**：见上面需求 C 那段，最小版得先有）

**不要先做的事**：加更多界面、换 UI 框架、做移动端适配、写更多文档。

---

## 15. 相关文件索引

| 文件 | 说明 |
| --- | --- |
| `项目交接文档.md` | **本文档**。给计划设计者的现状说明 |
| `src/data/topics.ts` | **核心内容资产**：9 套题目模板（里程碑 / 疑问 / 3 步建议 / 论文方向）。产品好不好主要看这里 |
| `src/services/papers.ts` | **论文推荐的唯一数据来源**（2026-09-23 抽出）。接后端时只改这一个文件，页面不用动，见 10.1 需求 C |
| `platform-ui-mockup(3).html` | **界面设计稿**（本地保留，未入库）。当前界面就是照它实现的 |
| `DESIGN.md` | 配色、布局、语义色规则（已更新到当前界面） |
| `README.md` | 运行方式、目录说明（已更新到当前界面） |
| `项目文档v1.md` | 上一版的产品设计：为什么砍掉项目地图、四条硬规则、明确不做的清单。**产品思路仍然有效** |
| `项目文档.md` | 最早的需求整理（本地保留，未入库） |
| `9.21周一大数据分析实践课程需要的数据/` | 9/21 课程交付物（本地保留，未入库） |
| `legacy-v1/` | 上一版实现 + 27 份往届学生材料（本地保留，未入库） |
| `legacy-v0/` | 更早那版实现（本地保留，未入库） |
