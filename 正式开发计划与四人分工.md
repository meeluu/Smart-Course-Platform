# 智慧课程平台正式开发总计划

> 版本：v2.0
> 编制日期：2026-09-23
> 时间基线：2026-09-23 开始，暂按 2026-11-30 交付规划；实际课程截止日待确认。
> 本文只记录完整开发步骤、目标、产出和验收标准，不预先细分其他三位成员的任务。推进到某个阶段后，再根据当时的工作量分配具体任务。

## 一、开发总目标

把当前“可以演示但刷新即丢失的 Vue 原型”逐步升级为一个可以真实使用的课程项目推进平台。

平台最终要形成这条闭环：

创建项目 → 保存项目状态 → 查看当前阶段 → 获得 AI 下一步建议 → 认领任务 → 提交证据 → 更新项目状态 → AI 重新给出下一步 → 论文检索和项目计划同步更新。

平台的产品边界保持不变：

- AI 给步骤、理由、完成标志和检索提示词，不直接替学生完成项目。
- 任务是否完成必须由学生提交证据并明确确认。
- 未确认的项目理解只能是草稿，不能直接成为项目事实。
- 论文可以由真实数据源提供可靠链接；大模型只生成检索提示词和研究方向，不凭空生成 DOI。
- 后端、数据库、认证、AI API 代理、部署和服务端依赖全部由负责人负责。
- 其他三位成员暂不预先固定详细文件边界，按当前阶段再分配前端、状态、内容、测试和文档工作。

## 二、第一目标：先完成最小可运行闭环

第一阶段不追求功能齐全，先完成最有价值的一条链路：

创建项目 → 刷新后项目仍在 → 提交证据 → 服务端调用大模型 API → 返回结构化的 1～3 条下一步建议 → 页面展示建议和依据 → 模型不可用时使用规则建议。

建议目标：

- 2026-09-30 前跑通第一条闭环；
- 2026-10-07 前完成稳定 MVP；
- 有 API key 时接入真实模型；
- 没有 API key、网络超时或模型返回错误时，规则建议仍然可以运行。

MVP 必须通过：

1. 至少创建两个项目，项目切换时数据互不污染。
2. 刷新页面后项目、证据、疑问、对话和当前项目仍然存在。
3. 认领任务不会虚增完成进度。
4. “记录进展”和“确认完成”是两个不同动作。
5. 提交证据后，时间线、疑问、任务状态和下一步建议能正确更新。
6. 大模型输出异常时，不会污染项目状态。
7. 前端代码中不存在真实 API key。
8. npm run typecheck 和 npm run build 通过。

## 三、阶段计划

### 阶段 0：基线确认与开发契约

时间：2026-09-23 ～ 2026-09-25

目标：让团队从同一个可运行版本开始，确认哪些内容已经存在，哪些内容属于本轮开发。

步骤：

1. 核对当前分支、远端、PR、main 保护规则和协作者权限。
2. 保留项目交接文档中已有的本地修改，不覆盖团队正在进行的源码。
3. 运行现有的 typecheck 和 build，记录当前基线。
4. 确认 API key 的提供方式、使用的模型服务和暂定预算。
5. 确认负责人独占后端的规则。
6. 确认前端、状态、内容和测试的接口约定，但暂不细分三位成员的长期任务。
7. 建立 GitHub Milestone：MVP、计划页、项目理解、服务端、协作与交付。

阶段产出：

- 一份可运行的基线记录；
- 一份接口契约；
- 一份环境变量说明；
- 一份 GitHub 开发约定；
- 一份当前问题清单。

验收：

- 已有本人任务分支就沿用；尚未创建时，从已合并基线的最新 main 创建；
- main 不允许直接提交；
- 负责人确认后端目录和服务端依赖只由自己修改；
- 未确认的需求不直接进入代码。

### 阶段 1：持久化、状态模型和最小 AI 代理

时间：2026-09-24 ～ 2026-09-30

目标：同时建立项目状态的可靠基础和第一条真实 AI API 调用链。

步骤：

1. 为 Project、Task、Evidence、Doubt、Recommendation 增加稳定 ID。
2. 建立数据版本和迁移机制。
3. 实现 localStorage 保存、恢复、损坏数据处理和当前项目恢复。
4. 把认领、记录进展、确认完成分开。
5. 让证据提交成为一次幂等操作，重复提交不会重复增加活跃度或完成状态。
6. 建立服务端最小 AI 代理。
7. 由服务端读取 OPENAI_API_KEY 或其他模型服务密钥。
8. 增加一个“根据当前项目状态生成下一步建议”的接口。
9. 增加模型输出结构校验。
10. 增加模型失败时的本地规则 fallback。
11. 页面增加 AI 加载中、成功、失败和使用规则建议的状态。

建议的最小调用链：

WorkbenchView 或 PapersView → Pinia action → 前端 API 适配层 → 服务端 advisor 路由 → AI provider → 模型 API。

本阶段后端范围（均由负责人负责）：

- server 目录；
- 服务端 API；
- 数据库或临时存储方案；
- AI provider；
- 服务端提示词调用；
- 结构化输出校验；
- 超时、限流、错误处理；
- .env.example 和密钥管理；
- 后端依赖和部署基础。

阶段产出：

- 刷新不丢数据；
- 可提交证据；
- 可调用一次真实模型；
- 无模型时可使用规则建议；
- 前端不接触密钥。

验收：

- 创建两个项目并刷新，数据仍然存在；
- API key 不出现在浏览器构建产物；
- 模型返回合法结果时显示建议；
- 模型超时、无 key、返回非法数据时使用规则建议；
- 证据已经保存时，模型失败不能回滚证据；
- MVP 代理先限本机开发；需要对外试用时，先落实调用鉴权和配额，不能暴露无保护的付费模型接口。

### 阶段 2：MVP 稳定与证据驱动建议

时间：2026-10-01 ～ 2026-10-07

目标：让平台可以进行一次完整、稳定、可演示的项目推进。

步骤：

1. 完善当前里程碑、任务状态和进度计算。
2. 让“认领任务”只改变任务状态，不直接增加完成比例。
3. 让“确认完成”必须满足必填字段和任务归属校验。
4. 提交证据后生成时间线。
5. 未确定内容进入未解决疑问。
6. 根据证据、疑问和已确认状态生成新建议。
7. 大模型建议和规则建议使用同一个 Recommendation 数据结构。
8. 已完成任务不能再次被推荐。
9. 建议必须说明 whyNow 和完成标志。
10. 页面支持重新判断下一步，但同一状态不能重复产生相同任务。
11. 对论文提示词生成保持兼容。
12. 进行一次完整演示和缺陷修复。

AI 在本阶段可以完成：

- 生成下一步建议；
- 解释为什么现在做；
- 总结证据；
- 生成论文检索提示词；
- 回答项目推进问题。

AI 在本阶段不能完成：

- 自动把任务设为完成；
- 自动解决疑问；
- 自动修改里程碑事实；
- 自动写入可靠论文链接；
- 读取其他项目或其他小组的数据。

阶段产出：

- MVP 标签；
- 完整演示脚本；
- 核心流程验收记录；
- 规则 fallback；
- AI 调用失败提示。

### 阶段 3：项目计划表和可视化

时间：2026-10-08 ～ 2026-10-16

目标：让学生可以在单独页面看到完整项目计划，并快速找到当前最重要的 2～3 件事。

计划页包含：

1. 阶段或里程碑；
2. 关键任务；
3. 建议负责人；
4. 任务状态；
5. 任务进度；
6. 完成标志；
7. 当前阶段标记；
8. 当前重点任务；
9. 任务与证据的关联；
10. 阶段推进条。

计划表不按 16 周展开，行的最高层级是阶段或里程碑。

计划数据采用混合方式：

- 第一阶段优先使用题目模板中已有的 3 条专属步骤；
- 其他阶段使用通用任务；
- 有 API key 时可以让大模型生成计划草稿；
- 模型生成的计划必须经过负责人或学生确认后才能进入正式计划；
- 计划页和工作台必须共用同一套任务 ID 和状态。

验收：

- 每个题目都有五个阶段；
- 当前阶段明显标出；
- 页面单独突出当前 2～3 个重点；
- 完成任务后计划表和工作台同步；
- 不新增第二套独立进度数据。

### 阶段 4：项目理解和材料处理

时间：2026-10-17 ～ 2026-10-23

目标：恢复 v1 中最重要的“理解确认门禁”。

四块项目理解：

1. 项目目标；
2. 交付内容；
3. 当前问题；
4. 待确认事项。

大模型可以根据材料生成四块内容草稿，并返回来源片段。系统必须：

- 标记内容来自哪份材料；
- 标记系统推断、学生确认或学生修改；
- 支持确认、修改和删除；
- 未确认的草稿不能成为项目事实；
- 支持重新整理；
- 保存学生确认后的版本。

材料处理分阶段实现：

第一阶段只支持：

- 粘贴文本；
- md；
- txt；
- 已由后端抽取好的文本。

后续按试用需求选做，不作为本轮必交范围：

- PDF；
- DOCX；
- 压缩包；
- notebook；
- OCR；
- 向量检索。

验收：

- 新材料不会自动改变已确认状态；
- 学生确认前显示为草稿；
- 模型失败时仍能手工录入理解内容；
- 材料中的恶意指令不会覆盖系统提示词；
- 不同项目的材料不会混入同一次请求。

### 阶段 5：正式后端、数据库、认证和 AI 服务

时间：2026-10-24 ～ 2026-11-06

目标：把本地原型升级为真实可保存、可鉴权、可扩展的服务。

后端全部由负责人完成，包含：

- server 服务入口；
- API 路由；
- 数据库连接；
- 数据库迁移；
- 用户认证；
- 项目成员关系；
- 项目权限；
- 证据保存；
- 任务状态保存；
- 材料元数据；
- AI provider；
- 提示词和模型配置；
- 结构化输出校验；
- 缓存；
- 限流；
- 日志；
- 错误处理；
- 部署配置；
- 服务端依赖。

最低接口范围：

- 登录、退出和当前用户；
- 项目创建和项目列表；
- 项目详情；
- 任务认领；
- 证据提交；
- 疑问解决；
- 项目理解确认；
- 下一步建议；
- 项目问答；
- 论文检索提示词；
- 材料元数据；
- 项目邀请。

AI 服务要求：

1. API key 只能存在服务端环境变量或部署平台 secret。
2. 前端不能出现 VITE_OPENAI_API_KEY。
3. 模型名、Base URL、超时和 token 上限可配置。
4. 模型响应必须通过 schema 校验。
5. 请求必须带项目 revision。
6. 项目已切换或 revision 已变化时，旧响应不能覆盖新状态。
7. 同一状态重复请求要使用缓存。
8. 429、5xx、超时和非法 JSON 都要有 fallback。
9. AI 不得读取其他项目的数据。
10. AI 不能决定任务是否完成。

### 阶段 6：多人协作和真实附件

时间：2026-11-07 ～ 2026-11-13

目标：支持 3～5 人小组在同一个项目中协作，并保留成员身份和证据作者。

功能：

- 登录；
- 加入项目；
- 邀请成员；
- 成员列表；
- 建议负责人；
- 证据提交人；
- 权限控制；
- 项目内数据隔离；
- 附件上传；
- 附件下载；
- 附件大小和类型限制；
- 上传失败重试；
- 证据与附件关联。

并发要求：

- 不允许用整份 Project 覆盖保存；
- 追加操作使用唯一 submissionId；
- 更新操作带 revision；
- 发生冲突时返回 409；
- 不静默覆盖别人的更新；
- MVP 不做 WebSocket，先使用操作后刷新或定时刷新。

### 阶段 7：测试、部署和安全检查

时间：2026-11-14 ～ 2026-11-23（集中回归；各阶段开发时已同步测试）

目标：确保现场演示和实际试用稳定。

测试范围：

- 创建项目；
- 多项目切换；
- 刷新恢复；
- 损坏 localStorage；
- 重复证据提交；
- 任务状态推进；
- 建议去重；
- AI 超时；
- AI 非法 JSON；
- AI 限流；
- prompt injection；
- 项目权限；
- 附件权限；
- 并发更新；
- 页面窄屏布局；
- 论文链接和提示词；
- 部署重启后的数据恢复。

必须通过：

- npm run typecheck；
- npm run build；
- npm run lint；
- 前端核心流程测试；
- 后端接口测试；
- AI schema 测试；
- fallback 测试；
- 无密钥扫描；
- 无私密课程资料扫描。

真实 API 测试只在本地或受保护环境进行，普通 PR 使用 mock provider，避免把模型费用和密钥带进 CI。

### 阶段 8：功能冻结和最终交付

时间：2026-11-24 ～ 2026-11-30

目标：停止扩展功能，集中完成交付。

步骤：

1. 冻结新增功能；
2. 修复阻塞性缺陷；
3. 完成部署；
4. 准备演示账号；
5. 准备演示项目；
6. 准备从创建项目到提交证据的演示脚本；
7. 准备模型不可用时的备用演示；
8. 整理使用说明；
9. 整理系统架构图；
10. 整理 AI 调用流程图；
11. 整理已知限制；
12. 完成最终彩排。

最终交付至少包含：

- 可运行前端；
- 可运行后端；
- 数据库迁移；
- 环境变量示例；
- 部署说明；
- 用户使用说明；
- API 接口说明；
- AI 提示词和 schema 版本记录；
- 测试记录；
- 演示脚本；
- 已知问题清单。

## 四、大模型 API 的使用原则

大模型 API 重点用于：

- 项目材料理解；
- 项目目标和交付内容抽取；
- 下一步建议；
- 证据摘要；
- 项目问答；
- 论文检索提示词；
- 计划任务草稿；
- 状态解释；
- 后续的材料章节总结。

规则代码重点负责：

- 项目事实；
- 任务状态；
- 进度计算；
- 证据保存；
- 疑问状态；
- 权限；
- 去重；
- 版本冲突；
- 是否确认完成；
- API 失败时的兜底。

原则是：

> 模型负责提出候选，系统负责验证，学生负责确认，后端负责保存。

## 五、GitHub 协作原则

只保留以下简单规则：

1. main 只通过 PR 修改。
2. 已有本人任务分支继续使用，不改名、不重复创建；没有时才从最新 main 创建，使用首轮任务文档里的建议名称。
3. 每个 PR 只完成一个阶段内的小目标。
4. 合并前必须通过 typecheck 和 build。
5. 负责人独占后端文件和服务端依赖。
6. 其他三位成员不直接修改 server 和数据库迁移。
7. 前端需要后端接口时先写清请求、返回和错误格式，再由负责人实现接口。
8. 任何真实 API key、.env、数据库文件和课程私密材料都不得提交。
9. 不在同一 PR 中混合重构、换框架和新增业务功能。
10. 按依赖推进并验收；不依赖前序结果的工作可以并行。
11. 每到一个阶段，由负责人再确定任务和文件边界；同一时间同一文件只有一个修改者，共享接口先约定再并行开发。

## 六、当前应该从哪里开始

现在只做阶段 0 和阶段 1：

1. 负责人确认 GitHub 基线和协作权限。
2. 负责人确定模型服务、API key 配置方式和预算上限。
3. 团队先确定 Task、Evidence、Recommendation 字段和接口请求、响应格式。
4. 负责人建立最小 server 服务。
5. 负责人实现一个下一步建议 API；前端可以根据同一契约并行使用模拟数据开发。
6. 团队把 localStorage 持久化补上。
7. 团队把提交证据和任务状态分开。
8. 团队把工作台接上 loading、成功、失败和 fallback 状态。
9. 先用一个题目完成完整演示。
10. MVP 稳定后，再进入项目计划表和材料理解。

暂时不要做：

- 全部题目的内容扩写；
- PDF/DOCX 自动解析；
- 实时协作；
- 向量数据库；
- 移动端；
- 复杂权限；
- 流式聊天；
- 自动生成可靠论文链接；
- 任何不影响 MVP 的视觉重构。


## 七、当前阶段的任务安排

负责人已具备服务器和 API key。本轮四人任务、文件边界、合并依赖和可复制的智能体提示词见 [首轮 MVP 任务分配与智能体提示词](首轮MVP任务分配与智能体提示词.md)。此安排只适用于第一条 AI 闭环，后续阶段仍按进度重新分配。

负责人承担全部后端；雍蕾负责工作台页面；李焰彬负责前端类型、状态、持久化和浏览器 API 适配；吴佳璐负责内容、提示词规格及脱敏评估样例。具体允许路径以首轮任务文档为准。
