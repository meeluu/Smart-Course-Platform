# 智慧课程平台服务器部署现状

> 记录时间：2026-09-25（北京时间）。本文件记录本次部署对话中实际看到的配置、命令输出和页面检查结果；服务器状态可能随之后的操作变化。隧道令牌、密码不写入本文档。

## 当前访问链路

```text
浏览器访问 https://course.xinxian-music.xyz
    → Cloudflare 的 course-wzh-jhl 隧道
    → 服务器 sinnet 上的 course-wzh-jhl-cloudflared.service
    → http://127.0.0.1:80（HTTP Host: course.xinxian-music.xyz）
    → Nginx → /home/w-4090/wzh-jhl/frontend
```

后端接口链路：

```text
浏览器/前端 → https://api.xinxian-music.xyz
    → 原有 Cloudflare API 隧道
    → http://127.0.0.1:8080
    → /home/w-4090/wzh-jhl/backend/dist/src/index.js
```

| 项目 | 当前状态 |
| --- | --- |
| 前端域名 | `https://course.xinxian-music.xyz`，外网首页实测 `200 OK` |
| 服务器登录 | `w-4090@119.255.238.113`；主机名 `sinnet` |
| 前端静态文件 | `/home/w-4090/wzh-jhl/frontend/`，包含 `index.html`、`assets/` 和图标 |
| 前端 Nginx 站点 | `/etc/nginx/sites-available/course.xinxian-music.xyz`，已在 `sites-enabled/` 启用 |
| 前端隧道 | Cloudflare 中的 `course-wzh-jhl`；已发布应用路由为 `course.xinxian-music.xyz` → `http://127.0.0.1:80`，匹配所有路径 |
| 前端隧道服务 | `course-wzh-jhl-cloudflared.service`，本次检查为 `enabled`、`active (running)`，日志中有 4 条已注册连接 |
| API 隧道 | 另有 `wzh-jhl-xinxian-api-tunnel`，控制台配置 `api.xinxian-music.xyz` → `http://127.0.0.1:8080`；现有 `cloudflared.service` 正在运行，未在本次部署中修改 |
| 后端目录 | `/home/w-4090/wzh-jhl/backend`，已上传 `dist/` 和 `package.json` |
| 后端监听 | `127.0.0.1:8080`；只通过原有 API 隧道对外提供接口 |
| 后端运行方式 | 已配置独立的 systemd 后端服务，当前服务正常运行并随系统启动 |

Cloudflare 中原先的 `course` A 记录指向 `119.255.238.113`，本次为创建隧道路由删除了这条记录。路由向导提示会自动配置 DNS；外网 HTTPS 已返回 200，但自动生成的 DNS 记录类型和值没有单独截图核对。后端接口已在服务器本机完成联调，`api` 域名的公网健康检查仍待单独确认。

## 本次验证结果与边界

1. `sudo nginx -t` 成功；输出同时包含服务器原有的 `jp.meiktv.com` 重复 `server_name` 警告，本次没有改动那些站点。
2. 服务器本机执行 `curl -I -H 'Host: course.xinxian-music.xyz' http://127.0.0.1/`，返回 `200 OK`。
3. 新隧道的手动连接日志显示 4 条 `Registered tunnel connection`，DNS、UDP、TCP、Cloudflare API 连通性检查均为 `PASS`。
4. 独立 systemd 服务启动后为 `active (running)`、`enabled`，其日志也显示 4 条 `Registered tunnel connection`。
5. 本地访问 `https://course.xinxian-music.xyz/` 和 `/papers` 的 HTTP 响应均为 `200 OK`；用户反馈浏览器页面正常显示。
6. **实际论文页路由是 `https://course.xinxian-music.xyz/#/papers`**：仓库当前使用 Vue 的 `createWebHashHistory()`。`/papers` 返回 200 只证明 Nginx 返回了前端页面文件，不代表 Vue 进入了论文页；`/#/papers` 尚未单独复测。
7. 后端本地接口 `POST /api/advisor/recommendations` 已完成一次 DeepSeek 联调：`HTTP_STATUS:200`、`SOURCE:model`、返回 3 条建议、证据引用校验通过，没有进入 `fallback`。

公网直连 `http://119.255.238.113/` 曾返回 `ADM/2.1.1` 的 403 页面，标题为“太和桥阻断”；Cloudflare 经旧 A 记录访问前端曾返回 522。当前前端通过隧道连接服务器，不依赖公网 80 端口放行。SSH 可连接该公网 IP，不代表其 80 端口可直达 Nginx。

## 服务器上的文件与服务

前端文件及令牌均位于 `/home/w-4090/wzh-jhl/`：

```text
/home/w-4090/wzh-jhl/
├── frontend/                # Vite dist/ 的内容；Nginx 只以此目录为网站根目录
│   ├── index.html
│   ├── assets/
│   ├── favicon.ico
│   └── favicon.svg
└── .course-tunnel/
    └── token                # 隧道令牌，权限设为 600；不要打印或提交到仓库
```

后端目录（已上传）：

```text
/home/w-4090/wzh-jhl/backend/
├── package.json
└── dist/
    └── src/index.js          # 后端入口
```

Linux 系统配置放在其标准位置：

- `/etc/nginx/sites-available/course.xinxian-music.xyz`：前端站点配置。监听 80，`server_name course.xinxian-music.xyz`，`root /home/w-4090/wzh-jhl/frontend`，`try_files $uri $uri/ /index.html`；静态资源配置了 7 天缓存。
- `/etc/nginx/sites-enabled/course.xinxian-music.xyz`：上述配置的启用链接。
- `/etc/systemd/system/course-wzh-jhl-cloudflared.service`：独立前端隧道服务。以 `w-4090` 用户执行 `/usr/local/bin/cloudflared tunnel run --token-file /home/w-4090/wzh-jhl/.course-tunnel/token`，失败时自动重启，随系统启动。服务文件不包含令牌值。

后端运行时配置（只存在于启动后端的进程环境中，不写入项目文件）：

```text
ADVISOR_PROVIDER=openai-compatible
MODEL_API_URL=https://api.deepseek.com/chat/completions
MODEL_NAME=deepseek-flash
```

鉴权使用 `Authorization: Bearer <API Key>`。API Key 不记录在本文档、项目目录或 Git 中。

本次看到的 `cloudflared` 版本为 `2026.7.3`。Cloudflare 控制台提示有更新，但连接检查通过；本次没有升级现有程序。

## 后续更新前端

在本地项目目录 `D:\智慧课程平台` 构建：

```powershell
npm run typecheck
npm run build
```

把 `dist` **目录中的内容**上传到现有的前端目录，不要把整个项目或令牌上传进去：

```powershell
scp -r "D:\智慧课程平台\dist\." w-4090@119.255.238.113:/home/w-4090/wzh-jhl/frontend/
```

上传静态文件不需要重启 Nginx 或隧道。旧版带哈希的 `assets` 文件可能留在目录中；不要对共享目录执行递归清理。完成后在浏览器检查首页及 `/#/papers`。

## 日常检查

在服务器上检查前端隧道服务：

```bash
systemctl status course-wzh-jhl-cloudflared.service --no-pager
sudo journalctl -u course-wzh-jhl-cloudflared.service -n 50 --no-pager
curl -I -H 'Host: course.xinxian-music.xyz' http://127.0.0.1/
```

检查后端是否仍在运行：

```bash
sudo ss -ltnp '( sport = :8080 )'
curl -sS http://127.0.0.1:8080/health
```

检查 API 域名（仅在确认后端进程运行时）：

```powershell
curl.exe -i https://api.xinxian-music.xyz/health
```

在本地检查外网入口：

```powershell
curl.exe -I https://course.xinxian-music.xyz/
```

`cloudflared.service` 是服务器原有的 API 隧道服务；排查前端时使用完整的 `course-wzh-jhl-cloudflared.service` 名称，避免操作 API。Nginx 同时承载其他站点；修改其配置时先运行 `sudo nginx -t`，并核对改动只涉及 `course.xinxian-music.xyz`。本次启动过一个前台测试用的 `cloudflared` 进程，随后指导退出；其最终进程数量未通过进程列表单独核实。

## 尚未完成或验证的部分

- 当前仓库是 Vue/Vite 前端，数据保存在浏览器运行内存中，刷新会重置；没有接入后端持久化。页面可访问不代表 API 或 AI 功能已经部署完成。
- 现有 API 隧道在 Cloudflare 控制台配置到本机 8080；后端本机健康接口、DeepSeek 模型联调、CORS 与公网 `api` 域名均已验证。
- 后端尚未配置独立的 systemd 服务；当前 shell/`nohup` 进程退出或服务器重启后，需要单独启动后端。后续如配置自启动，应只新增智慧课程平台自己的服务单元，不修改现有 `cloudflared.service` 或其他服务。
- 前端隧道目前只有这一台服务器连接；服务器、Nginx 或 `course-wzh-jhl-cloudflared.service` 停止时，前端网站会不可访问。
